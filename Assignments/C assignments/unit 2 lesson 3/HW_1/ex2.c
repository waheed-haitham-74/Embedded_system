#include <stdio.h>
#include <stdlib.h>

int main()
{
    int number;
    printf("enter an integer :");
    fflush(stdin);fflush(stdout);
    scanf("%d",&number);
    printf("the number is :%d",number);
}
